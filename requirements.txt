httpx>=0.28
# torchaudio>=2.9 drops its built-in wav decoder in favor of the separate
# torchcodec package - silero-vad's read_audio() (test fixture loading only)
# then fails with "torchcodec is required". Capped below 2.9 to keep the
# decoder torchaudio.load() already works with, matching the torch version
# verified in PROJECT.md's day-0 environment.
torch>=2.8,<2.9
torchaudio>=2.8,<2.9
silero-vad>=6.2
soundfile>=0.14
sounddevice>=0.5
numpy>=1.26
silero>=0.5
num2words>=0.5
piper-tts>=1.4
mss>=10.2
pyperclip>=1.11
pywebview>=6.2
pytest>=8.0
pytest-asyncio>=0.24
